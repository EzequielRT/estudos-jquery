$(function() {
    
    // Adiciona evento de clique
    $('button').bind('click',
        function() { $(this).toggleClass('fundovermelho') } 
    );

    /*
    // Adiciona evento de clique
    $('button').on('click',
        function() { $(this).toggleClass('fundovermelho') } 
    );
    */

});